# Sample Dataset

This directory contains a small tracked sample derived from the processed Databento
conversion outputs so contributors can test the project without the full raw dataset.

Current sample:

- Symbol: `AAPL`
- Day: `20260302`
- Session window: `12:00:00` to `12:05:00` America/New_York

The files mirror the processed dataset layout used by the project:

- `ask.csv`
- `bid.csv`
- `price.csv`
- `msg.csv`
- `trades.csv`
