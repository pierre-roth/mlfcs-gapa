[project]
name = "mlfcs-gapa"
version = "0.1.0"
description = "Databento preprocessing and experiments for market making from limit order books."
requires-python = ">=3.9"
dependencies = [
    "rich>=13.9,<14",
]

[tool.uv]
package = false
