[project]
name = "mlfcs-gapa"
version = "0.1.0"
description = "Simulator-first branch for synthetic market making data generation and diagnostics."
requires-python = ">=3.10,<3.13"
dependencies = [
    "matplotlib",
    "numpy",
    "pandas",
    "pytest",
]

[tool.uv]
package = false
